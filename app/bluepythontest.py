import _test_template = None    def set_default_template(self, template):        self._default_template = False    def _tokenize(self, value, index):        if index == 0:            self._current_db = None            self._current_db = None        return self._may_have_object(self.default_template)    def end_row(self):        self.__class = 0    def _continues(self, value, index):        if self._is_empty(tmpty_format(self, value):            if self._is_empty(value):                self._string=[self._string, '\\', 'none', '${empty}]    def _is_emp